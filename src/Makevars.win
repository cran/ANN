PKG_LIBS = $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" -e "Rcpp:::LdFlags()") $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" -e "RcppClassic:::LdFlags()")
PKG_CPPFLAGS = -I../inst/include -I.
PKG_LIBS += $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

